注意：字段需严格按照es表字段设计（es表字段设计.md）。
左侧区域：
1、点击左侧客群列表项，中间对话框显示【单个客群查询结果 ai-message-segment-result】。现在没有分析结果，是让用户重新提问，这个不对。注意，【单个客群查询结果】，应该有两个状态，用户对话查询出来的，标题是客群查询结果，有保存为客群按钮。如果是从左侧客群列表项点击，然后对话框显示的，标题是客群名称，没有保存为客群按钮。
2、【单个客群查询结果】卡片点击上方热区后，右侧区域，要展示【单个客群分析详情】，内容根据es表字段设计。
3、勾选了客群之后，点击对比之后，中间对话显示，将现在展示的【单个客群查询结果 ai-message-segment-result】修改为【简要客群对比结果 ai-message-segment-comparison】（需要新创建）。点击之后，右侧需要有【详细客群对比结果】。卡片和详情内容需要按照es表字段新设计。

右侧：
1、客户详情中的字段内容，按照表字段重新设计，和es表字段保持一致。
2、根据上述需求，右侧详情应该有以下几种情况：【客户详情】、【单个客群分析详情】、【详细客群对比结果】。
